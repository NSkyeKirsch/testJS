var strOne = window.prompt("First string: ");
var strTwo = window.prompt("Second string: ");

function combine(one, two){
  return one + two;
}

var result = combine(strOne, strTwo);

console.log(result);
